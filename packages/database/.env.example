# Database Configuration
# ⚠️ NEVER commit the actual .env file to Git!
# This is just a template - copy to .env and fill with real values

# PostgreSQL Connection (Production)
# Use SSL in production, connection pooling, and strong passwords
DATABASE_URL="postgresql://username:password@localhost:5432/chatbot?schema=public&sslmode=require&connection_limit=10"

# Security Notes:
# 1. Use strong passwords (32+ chars, random)
# 2. Always use SSL in production (sslmode=require)
# 3. Limit connection pool size
# 4. Use read replicas for scaling
# 5. Enable PostgreSQL audit logging
# 6. Rotate credentials regularly (every 90 days)
# 7. Use separate DB users for different apps (web vs api)
# 8. Never use 'postgres' superuser in application code

# Example Production URL:
# DATABASE_URL="postgresql://app_user:STRONG_RANDOM_PASSWORD_HERE@your-db-host.com:5432/chatbot_prod?schema=public&sslmode=require&connection_limit=10"

# Development (Local)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/chatbot_dev?schema=public"
